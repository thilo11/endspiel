# ── Rust / Cargo build artifacts ──
target/
Cargo.lock
**/*.rs.bk
.cargo/config.toml

# ── Editors, IDEs, OS cruft ──
*.swp
*.swo
*~
.vscode/
.idea/
.DS_Store
.envrc

# ── Python ──
__pycache__/
*.pyc
.venv/

# ── Local-only data, assets & nets (books, tablebases, training data) ──
assets/
resources
/data/
train/checkpoints/
*.bin
*.txt
*.csv
*.pgn

# ── Build / run outputs & release artifacts ──
/dist
/games
endspiel-*
puzzle_results/

# ── Local-only tooling & scratch (incl. lichess-bot analysis) ──
.claude/
/lichess
debug_*.py
debug_*.sh
test_*.sh

# ── Local config & tuning output ──
config.json
tuned_params.json
spsa_checkpoint*.json
spsa_result.json
