# ── Rust / Cargo build artifacts ──
target/
Cargo.lock
**/*.rs.bk
.cargo/config.toml

# ── Editors, IDEs, OS cruft ──
*.swp
*.swo
*~
.vscode/
.idea/
.DS_Store
.envrc

# ── Python (lichess-bot analysis scripts) ──
__pycache__/
*.pyc
.venv/

# ── Local-only assets & nets (books, tablebases) ──
assets/
resources

# ── Build / run outputs & release artifacts ──
/dist
endspiel-*

# ── Local-only tooling & scratch (incl. lichess-bot analysis) ──
.claude/
/lichess
debug_*.py
debug_*.sh
test_*.sh

# ── Local config & tuning output (chess-tuner) ──
config.json
tuned_params.json
